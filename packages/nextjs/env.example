# ========================================
# Web3.Storage / Storacha (Required for image upload - 5GB FREE!)
# ========================================
# Setup via CLI:
# 1. npm install -g @storacha/cli
# 2. storacha key create
#    → Copy: did:key:z6Mk... (Agent DID)
#    → Copy: MgC... (Private Key) → paste below
# 3. storacha login your@email.com (verify email)
# 4. storacha space create Zameme
# 5. storacha delegation create <AGENT_DID_from_step2> \
#      --can space/blob/add --can space/index/add \
#      --can filecoin/offer --can upload/add --base64
#    → Copy base64 proof → paste below
W3STORAGE_PRIVATE_KEY=MgC...your_private_key
W3STORAGE_PROOF=mAYIEAIw...your_base64_proof

# ========================================
# WalletConnect Project ID (Required for RainbowKit)
# ========================================
# Get free Project ID at: https://cloud.walletconnect.com/
# 1. Sign up at https://cloud.walletconnect.com
# 2. Create new project
# 3. Copy Project ID
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# ========================================
# Contract Address (Optional - auto-generated after deploy)
# ========================================
# Will be updated automatically after running 'pnpm deploy:localhost'
# Or manually set after deploying to Sepolia
NEXT_PUBLIC_CONTRACT_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3

# ========================================
# Network Configuration (Optional)
# ========================================
# Use 'localhost' for local development or 'sepolia' for testnet
NEXT_PUBLIC_NETWORK=localhost

# Custom RPC URLs (Optional - only if you have private endpoints)
NEXT_PUBLIC_SEPOLIA_RPC_URL=https://eth-sepolia.public.blastapi.io
NEXT_PUBLIC_LOCALHOST_RPC_URL=http://127.0.0.1:8545

# ========================================
# Relayer Private Key (Required for batching deposits)
# ========================================
# Create new wallet for relayer:
# 1. In MetaMask: Create new account → Export private key
# 2. Fund with small amount of ETH for gas
# 3. Paste private key below
RELAYER_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

